% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/claude.R
\name{clip_read}
\alias{clip_read}
\title{Read the clipboard into an R object}
\usage{
clip_read(
  format = c("auto", "html", "rtf", "text", "csv"),
  as = c("auto", "gt", "tibble", "character")
)
}
\arguments{
\item{format}{One of \code{"auto"}, \code{"html"}, \code{"rtf"}, \code{"text"}, \code{"csv"}.
\code{"auto"} inspects available formats and picks the richest usable one.}

\item{as}{Target R representation: \code{"auto"}, \code{"gt"}, \code{"tibble"},
\code{"character"}.}
}
\value{
A \code{gt_tbl}, a tibble, or a character vector, depending on \code{as}
and what was found on the clipboard.
}
\description{
Inspects what's actually on the clipboard and parses it into the
requested (or best-guess) R representation. HTML tables become \code{gt}
tables when the gt package is available; RTF is a lossy fallback.
}
\examples{
\dontrun{
# Copy a table from Excel, then:
tbl <- clip_read()
}
}
