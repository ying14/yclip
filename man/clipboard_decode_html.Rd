% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect.R
\name{clipboard_decode_html}
\alias{clipboard_decode_html}
\title{Decode Windows HTML Clipboard data}
\usage{
clipboard_decode_html(x)
}
\arguments{
\item{x}{A raw vector containing the complete Windows \verb{HTML Format}
clipboard representation.}
}
\value{
A character string containing the HTML fragment between the
\code{StartFragment} and \code{EndFragment} byte offsets.
}
\description{
Windows stores HTML on the clipboard using the \verb{HTML Format} clipboard
format. The stored data consists of an ASCII header containing byte
offsets such as \code{StartFragment} and \code{EndFragment}, followed by the HTML
data itself.
}
\details{
This function extracts the HTML fragment identified by those offsets and
returns it as a character string. It therefore converts the raw Windows
clipboard representation into the HTML content that can be used directly
by R or an HTML renderer.

In contrast, \code{\link[=clipboard_read_raw]{clipboard_read_raw()}} returns the complete \verb{HTML Format}
representation, including the Windows clipboard header. This function is
used internally by \code{\link[=clipboard_read_text]{clipboard_read_text()}} when \code{format_name} is
\code{"HTML Format"}.
}
\keyword{internal}
