% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rstudio_code.R
\name{render_code_html}
\alias{render_code_html}
\alias{syntax_highlighting}
\title{Render code to a self-contained HTML fragment, optionally executing it}
\usage{
render_code_html(
  code,
  language = "r",
  execute = TRUE,
  style = "pygments",
  background = "#eeeeee",
  collapse = TRUE,
  knitr_opts = list(),
  pandoc_opts = character(),
  envir = parent.frame()
)

syntax_highlighting(
  text_code,
  language,
  style = "pygments",
  background = "#eeeeee",
  pandoc_opts = character()
)
}
\arguments{
\item{code}{Character vector of source lines, or a single string.}

\item{language}{Language identifier. When executing, must be a language
knitr can run (currently R).}

\item{execute}{Whether to run the code and capture output/figures.}

\item{style}{Pandoc highlight style, e.g. \code{"pygments"}.}

\item{background}{CSS background color for the source-code block, or \code{NULL}.}

\item{collapse}{Logical; if \code{TRUE} (the default), interleave source code and
printed output in one code block using knitr's \code{collapse} option. Output
lines are prefixed with \verb{#>}.}

\item{knitr_opts}{Named list of knitr chunk options (used only when
executing), e.g. \code{list(fig.width = 6, dpi = 150)}. Values here override the
defaults set internally (including \code{fig.cap}). To list all
options, run \code{knitr::opts_chunk$get()}. These are described at:
\href{https://yihui.org/knitr/options/}{}}

\item{pandoc_opts}{Character vector of extra Pandoc arguments.}

\item{envir}{Environment in which to evaluate \code{code} when \code{execute = TRUE}.
Defaults to the calling environment.}
}
\value{
A character string containing a self-contained HTML fragment.
}
\description{
Converts a block of source code to an HTML fragment suitable for writing to
the clipboard as \code{"HTML Format"}. When \code{execute = TRUE} and \code{language} is one
knitr can run (currently R), the code is executed and its output and figures
are captured (as in a Quarto/R Markdown chunk). When \code{execute = FALSE}, the
code is only syntax-highlighted via Pandoc, which allows any language Pandoc
can highlight (e.g. \code{"rust"}, \code{"python"}) even if knitr cannot run it.
}
\details{
The result is a fragment (Pandoc-generated \verb{<style>} blocks plus content),
not a full HTML document. Figures are embedded as data URIs.
}
\examples{
\dontrun{
library(ggplot2)

code <- '
ggplot(mtcars, aes(wt, mpg, color = factor(cyl))) +
  geom_point(size = 3) +
  labs(
    title = "Fuel economy by vehicle weight",
    x = "Weight (1000 lbs)",
    y = "Miles per gallon",
    color = "Cylinders"
  ) +
  theme_minimal()
'

# Render code, plot, and any printed output as an HTML fragment. The plot
# is rendered at 10 x 5.625 inches (16:9) and 300 dpi before being embedded
# in the returned HTML.
html <- render_code_html(
  code,
  options = list(
    fig.width = 10,
    fig.height = 5.625,
    dpi = 150,
    dev = "jpg"
  )
)

view_html(html)

clipboard_write_formats(
  "HTML Format" = html
)

# Highlight code without evaluating it. Any language recognized by Pandoc
# can be used when execute = FALSE.
python_html <- render_code_html(
  "x = [1, 2, 3]\nprint(sum(x))",
  language = "python",
  execute = FALSE
)
}
}
